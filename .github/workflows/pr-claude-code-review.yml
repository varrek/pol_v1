name: PR - Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issue_comment:
    types: [created]
  issues:
    types: [opened, assigned]

concurrency:
  group: ${{ github.workflow }}-pr-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: true

permissions:
  contents: write # Required for Claude to create PRs and commit changes
  pull-requests: write
  issues: write
  id-token: write
  actions: read # Required for Claude to read CI results on PRs

jobs:
  review:
    # Run on PR events, issue comments that mention @claude (including on issues without PRs),
    # or when issues are opened/assigned with @claude in title or body
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'issues' && (contains(github.event.issue.body, '@claude') || contains(github.event.issue.title, '@claude')))
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository (PR event)
        if: github.event_name == 'pull_request'
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Checkout repository (issue_comment event on PR)
        if: |
          github.event_name == 'issue_comment' &&
          github.event.issue.pull_request
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout PR branch (issue_comment event on PR)
        if: |
          github.event_name == 'issue_comment' &&
          github.event.issue.pull_request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh pr checkout ${{ github.event.issue.number }}

      - name: Checkout repository (issue_comment or issues event on regular issue)
        if: |
          (github.event_name == 'issue_comment' && !github.event.issue.pull_request) ||
          github.event_name == 'issues'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get PR base branch or default branch
        id: pr-info
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            echo "base_ref=${{ github.event.pull_request.base.ref }}" >> $GITHUB_OUTPUT
            echo "pr_number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
            echo "issue_number=" >> $GITHUB_OUTPUT
            echo "is_pr=true" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" == "issue_comment" ] && [ -n "${{ github.event.issue.pull_request }}" ]; then
            BASE_REF=$(gh pr view ${{ github.event.issue.number }} --json baseRefName -q '.baseRefName')
            echo "base_ref=$BASE_REF" >> $GITHUB_OUTPUT
            echo "pr_number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
            echo "issue_number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
            echo "is_pr=true" >> $GITHUB_OUTPUT
          else
            # For regular issues or issue comments on regular issues
            DEFAULT_BRANCH=$(gh repo view --json defaultBranchRef -q '.defaultBranchRef.name')
            echo "base_ref=$DEFAULT_BRANCH" >> $GITHUB_OUTPUT
            echo "pr_number=" >> $GITHUB_OUTPUT
            if [ "${{ github.event_name }}" == "issues" ]; then
              echo "issue_number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
            elif [ "${{ github.event_name }}" == "issue_comment" ]; then
              echo "issue_number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
            fi
            echo "is_pr=false" >> $GITHUB_OUTPUT
          fi

      - name: Claude Code Review
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            ${{ steps.pr-info.outputs.is_pr == 'true' && format('You are reviewing Pull Request #{0}.', steps.pr-info.outputs.pr_number) || format('You are responding to GitHub Issue #{0}.', steps.pr-info.outputs.issue_number || github.event.issue.number) }}
            
            ${{ steps.pr-info.outputs.is_pr != 'true' && format('**Issue Title:** {0}', github.event.issue.title) || '' }}
            ${{ steps.pr-info.outputs.is_pr != 'true' && format('**Issue Description:**\n{0}', github.event.issue.body) || '' }}
            
            Use the standards in .claude/agents/code-reviewer.md.

            **If this is a Pull Request:**
            1. Read .claude/agents/code-reviewer.md for the full checklist
            2. Run `git diff origin/${{ steps.pr-info.outputs.base_ref }}...HEAD` to see all changes
            3. Apply the review checklist to modified files
            4. Run `npm run lint` to check for linting issues (if applicable)
            5. Provide feedback organized by severity (Critical, Warning, Suggestion)
            6. Post your review as a comment on the PR

            **If this is an Issue:**
            1. Read and understand the issue description above
            2. Read .claude/agents/code-reviewer.md for coding standards
            3. If the issue requires code changes:
               - Create a new branch: `git checkout -b fix/issue-${{ steps.pr-info.outputs.issue_number || github.event.issue.number }}`
               - Make the necessary changes following the code review standards
               - Run `npm run lint` and fix any issues
               - Commit your changes with a clear message like: "Fix: ${{ github.event.issue.title }}"
               - Push the branch: `git push origin fix/issue-${{ steps.pr-info.outputs.issue_number || github.event.issue.number }}`
               - The workflow will automatically create a PR after you push
            4. If the issue is a question or doesn't require code changes:
               - Provide a helpful response explaining your answer
            5. **IMPORTANT**: Always respond to the issue. If you made code changes and pushed a branch, the PR will be created automatically. Otherwise, provide a helpful comment.
          claude_args: |
            --max-turns 15
            --allowedTools "Read,Glob,Grep,Bash(git:*),Bash(npm run lint*),Bash(npm run test:*)"
          
      - name: Create Pull Request for issue fix
        if: |
          steps.pr-info.outputs.is_pr == 'false' &&
          github.event_name != 'pull_request'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ steps.pr-info.outputs.issue_number || github.event.issue.number }}
          BRANCH_NAME: fix/issue-${{ steps.pr-info.outputs.issue_number || github.event.issue.number }}
        run: |
          # Check if branch exists and has commits
          if git ls-remote --heads origin "$BRANCH_NAME" | grep -q "$BRANCH_NAME"; then
            # Check if PR already exists
            if ! gh pr view "$BRANCH_NAME" --repo "${{ github.repository }}" 2>/dev/null; then
              gh pr create \
                --title "Fix: ${{ github.event.issue.title }}" \
                --body "Fixes #$ISSUE_NUMBER

              This PR was automatically created by Claude Code to address issue #$ISSUE_NUMBER." \
                --base main \
                --head "$BRANCH_NAME"
              
              # Comment on the issue
              gh issue comment "$ISSUE_NUMBER" \
                --body "I've created a PR to address this issue. The PR will be automatically created after the branch is pushed."
            fi
          fi
